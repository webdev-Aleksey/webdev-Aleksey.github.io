$(function () {
    // $('.slider').slick();
    $('.single-item').slick({
      dots:true,
      slidesToShow: 1,
      speed:600,
      autoplay:false,
      autoplaySpeed: 3000,
      adaptiveHeight: true,
      centerMode: false
      
  
    });
  
  });